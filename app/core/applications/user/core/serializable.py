from uuid import UUID
from typing import Optional, List, Any, Dict
from pydantic import BaseModel, Field


"""class RequestDocuments(BaseModel):
    product_uuid: str
    renew: List[str] = Field(default=[])
    application_uid: str
    contact_uuid: str
    overwrite: Optional[dict]
"""
